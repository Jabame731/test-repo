 // @if (
 //      lastMessage.messageSid === message.messageSid &&
 //      lastMessage.messageId === message.messageId &&
 //      message.deliveryStatus === 'success'
 //    ) {
 //      <p class="text-xs ml-1 text-gray dark:text-gray-400 mt-[0.5px] mr-2">Sent</p>
 //    }

//  @Input() lastMessage: MessageModel;

//   <ups-phone-text-message-chat-bubble [message]="message" [userNumber]="phoneNumberAssignment.phoneNumber"></ups-phone-text-message-chat-bubble>
// +      <ups-phone-text-message-chat-bubble [message]="message" [userNumber]="phoneNumberAssignment.phoneNumber"
// +      
// +        [lastMessage]="currentMessages[currentMessages.length-1]"
// +      
// +      
// +      ></ups-phone-text-message-chat-bubble>
//      }
//    </ul>
//  </div>
// +  notificationDeliveryFailed$ = new BehaviorSubject<MessageModel>(null);

